{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Convert sample data",
        "type": "go",
        "request": "launch",
        "mode": "debug",
        "program": "${workspaceFolder}/cmd/fmpxml-to-json/",
        "args": [
          "-input", "${workspaceFolder}/sample.xml",
          "-output", "${workspaceFolder}/sample.json"
        ]
      }
    ]
  }